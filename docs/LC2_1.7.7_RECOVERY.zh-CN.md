# 1.7.7 诊断内测恢复说明

记录日期：2026-09-08。生命周期：`diagnostic-internal / ui-gap-open / awaiting-real-game`。
r2诊断编码边界已修，r3实际管道回放与静态可见检查通过；后到复核发现一次85.863秒UI心跳空档。
r4补充自动主线程位置记录，原停顿原因仍未确认，不能按短时对照未复现宣布修复。
1.7.7 不作为正式分享版发布；达到可发包条件后，正式版本从 1.7.8 开始。

## 基线与已知边界

r1 指作者实际运行的 1.7.7 EXE，SHA256 为
`21ac890f96c2992520184f6dc41e5b0d532c301c46207bc16029363b1fc785bc`，构建源
`2b98bee57987a420be1a2653772f8177b7bf5e59`。它实际使用 Distribution profile；
诊断内测是版本生命周期归类，保留原字节、构建属性和失败记录。

已证实的机制是：512项队列满后清队列、停止接收，读取线程退出，消费器锁存错误；
后续合法数据不能再进入统计。原实机最初消费停滞的原因仍未确认；受控突发能复现
永久停收，有限窗口采样试验未证实其因果关系。恢复设计不依赖该触发假设成立。

## 恢复合同

- 单一后台消费线程负责验证与聚合，Tk 只刷新快照。聚合器写入与快照使用同一锁；
  对局归档按“控制器 → 归档 → 聚合器”顺序协调，导出不能截取领先于事件批次的摘要。
- 队列仍限512项，读取端最多等待250毫秒给消费器腾出空间。确实满时保留已有合法前缀，
  按顺序报告缺口，然后关闭本次连接并自动重连；不会永久退出读取线程。
- UTF-8、JSON、结构、身份、顺序或非有限数值不合法的记录单独隔离；后续合法记录继续。
  错误样本仅供诊断，不混入累计。重复记录仍不重复统计，外局数据仍需明确边界。
- 断流、序号缺口和同局重连持续标明本轮不完整，并清掉近10秒DPS差分基线。
  Mini 用琥珀色提示“数据有缺口”或“重连中”；后续新局重置质量状态。
  完整官方值可以校正造成伤害、Boss伤害和承伤，不能补造回血/耗蓝等缺失过程。
- 保留最近16个问题样本（每个最多4096字符）和8条合法记录上下文；每5秒保存队列深度、
  高水位、消费时间和UI心跳等紧凑状态。问题/恢复日志各限每秒一条，避免异常风暴拖慢消费。
  队列丢弃计数是本机未入队项目的下界，包含通知，不代表所有断流游戏事件的精确数量。
- 支持诊断单列当前传输故障和本轮缺口；`partial` 仍只表示收集是否完整。
  分享档不保存全量逐事件历史，但可导出有界异常参考样本；原隐私脱敏继续生效。

## 验证与后续

源码全套 `py -3 -m unittest discover -s tests -p "test_*.py" -q` 已运行401项，全通过、
无跳过（39.121秒，最后的候选序号/日志限频小改由构建前检查继续覆盖）。覆盖坏行前后续收、
强制溢出前缀保留/真实线程重连、Bridge错误后恢复、无UI消费的1002条突发、跨局身份、
非有限数拒绝、缺口/DPS/官方值、并发导出以及诊断质量项。

这些是源码与合成输入证据，尚不能宣布真实游戏问题收口。r2已按真实Diagnostic profile
从9c867d2构建，构建内391项全通过，Bridge编译0警告0错误，EXE SHA256为
`5b154949410a1215f0f100a2984cad671f613decee5f8f952e7175647ffcfce5`。构建后只读复核
发现JSON转义的孤立代理项会让诊断样本UTF-8编码报错，中断同批后缀并丢掉待归档前缀。
普通坏值与该坏字符正反例已复现，r2保留且不交付实测；该边界不是原实机初停根因。

后继修订将样本安全转义，并在聚合前隔离无效Unicode；实际JSON解码和直接入队的高/低
代理项、普通坏值、合法中文/emoji对照共38项传输/恢复聚焦测试全通过；ROOT独立运行新增
两个具名测试，也通过且无跳过。

r3从 `0e3b58ffc4db39af91fe70bb2fc597365deb7329` 构建，EXE SHA256为
`434c42b99a63317a4e0debdcee317f1b9f8529fa61cc6b71927d066ec23b772b`。
构建内393项测试通过、无跳过；此集合按原脚本排除三个public构建测试模块，不冒充完整发现集合。
Bridge编译0警告0错误，实际EXE及桌面副本自检、CLI支持导出退出0。候选为Diagnostic档，
三项诊断开关均true，候选序号3；Bridge仍为1.7.4。

| 实际EXE检查 | 结果与边界 |
| --- | --- |
| 原始1001条突发、相同100/s匀速输入 | 两臂原始前缀均478679字节、SHA `1da7b83c65cf1973ddb397a90a4b79559c681c0c34c11cf329382b761a929338`，与原r1失败控制输入逐字节一致；加一个明确结束记录后，各归档1002条、序号0–1001连续、总伤害1000、数据无缺口。突发高水位512、未入队计数0 |
| 损坏Unicode、非法UTF-8、NaN | 三条异常隔离后，合法伤害仍累计47、地图到黑城堡；实际支持ZIP保留3项异常、当前缺口，`partial=false`且findings明确报告质量问题 |
| Bridge错误后重连 | 断开时保留前缀11，第二次连接后累计28（归档伤害11+17）；Mini由“重连中”变为持续“数据有缺口”，房间正常更新 |
| 相关可见状态 | Mini缺口、紧凑主窗缺口、Mini重连三项完整PID/HWND/PNG/几何回执通过；恢复后同窗像素、支持导出完成弹窗也已检查。布局和指标顺序保留 |
| 桌面交付 | 新独立目录共1774个文件、167274688字节，全部大小/SHA回读一致；不带合成回放的对局导出，原r1/r2均保留 |

原来源弹窗已在同一个冻结public-core上补齐实际PID/HWND，完整PNG与旧参考逐字节相同；
r3支持导出弹窗也有完整窗口身份。三个相关弹窗处理函数相对2b98bee的AST一致。
原5个未改布局臂保留原验证范围，不重新宣称原实战失败包可发布。

构建与回放期间曾有输出目录变量、截图时限、错误启动入口和管道fixture过早断开的测试工具问题；
均保留记录并在正确输入/定位后完成对应检查，没有据此改产品或降低断言。以上均为受控回放，
不等于真实游戏全局通过。后续仍须完成真实游戏输入、未解释UI停顿的闭合，以及1.7.8的实际产物与发布核验。

## r3后到复核与r4诊断补充

r3的重连Mini臂存在85.863秒UI心跳间隔。该段后台健康记录持续、processed从11到12，
归档也保留序号0–11全部事件；因此接收/归档与静态像素结论保留，但不覆盖持续UI响应。
第二次PrintWindow截图与空档相邻，后来正常退出操作附近心跳恢复；这是时间关联，原因未知。

随后两组源码、两组原r3 EXE有限对照均正常自动退出，没有复现长空档。源码两臂不添加Tk定时器，
以独立线程记录心跳和Python主线程位置；EXE两臂共享第一次截图/QA回执，仅改变是否再次截图。
这些约25秒观察窗不能排除真实游戏或其他时序条件下复发。原异常保持OPEN，不重新跑已通过的全矩阵。

r4仅补充诊断：后台原有5秒健康记录发现UI心跳已超过3秒时，附上主线程最多24层的文件、
函数与行号，不调用Tk、不读取源文件或局部变量。正常心跳不采样，采样不可用仍保留原健康记录。
日志仍受既有大小上限与脱敏约束。83项支持诊断、恢复与入口聚焦测试通过、无跳过；包含真实后台线程
抓取等待中主线程的正控、正常/无关事件不采样及采样失败保留健康数据的反例。r4不是该UI停顿的修复结论。
