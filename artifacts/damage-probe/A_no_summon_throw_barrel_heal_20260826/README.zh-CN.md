# A 组：单人、无召唤物，含投掷/爆炸桶/受伤后回血

记录时间：2026-08-26（Asia/Shanghai）

## 作者给出的真实条件

- 单人，无召唤物。
- 选择宝藏“雷龙翼膜”（闪避时对附近随机敌人施放雷击）。
- 第一关未受伤。
- 第二关使用飞镖道具，作者确认其为投掷伤害；攻击爆炸桶后被炸倒，但观察上似乎没有损失生命。
- 第三关被哥布林击中，受伤后似乎回复了 1 点生命。
- 结算截图：造成伤害 `34486`、承受伤害 `46`、对首领伤害 `0`、击杀魔物 `35`。

因此本样本可以作为“无召唤物”对照，但不是只有普通攻击的纯基线；投掷、环境爆炸、闪避雷击和受伤后回复必须保留为解释变量。

## 冻结身份

- 最终日志：`A_log_20260826-210040_final.log`
- 日志字节：`78,802`
- 日志 SHA-256：`FFF574C53DA4528806BF197FA2DCDF0C091F3C4555D9C19C875E3A4B9B8D4728`
- 游戏在冻结时已经退出；这不是 live snapshot。
- 结算截图 SHA-256：`1A98F4466C2D2CEA71959324C22B44AA1651152AECE09CFE453ABAFD2D330856`
- 其余三张条件截图 SHA-256：
  - `EB24883344636D5291A72E48A321916DD0995E46EFE6665A497D1FDF10CDE402`
  - `067DC8C56767D3A49AF049F8E69DCFD0A3A79C1FDF3DCD4A4B786994824BA5AD`
  - `A06E198E65DD355537E4217A5759C3D94195CF8C63A0434B918838E3BD3678D0`

## 探针结果

- `[LC2DAMAGE]`：`176` 条；hit `174`、boundary `2`、probe error `0`。
- `official_attacker`：`172` 条，逐击 `applied` 诊断和 `41231.479`。
- `official_defender` 与 `monster_record` 各 `1` 条，均为同一指纹、`45.158`；再次证明跨 path 不可相加。
- 官方结算伤害 `34486` 与逐击诊断和相差 `6745.479`（官方值的 `19.56%`）。没有召唤物时差值仍然存在，因此 B 组差值不是噩梦娃娃独有。
- 结算击杀 `35`；攻击事件中 `lethal=True` 为 `33` 条。差值约为每个结算击杀 `192.728`，与“致死一击包含超出目标剩余 HP 的过量部分”相容，但当前未记录命中前 HP，尚不能证明。
- 最后一个房间边界字段：`normal=12428.79, skill=0, throw=298.471`。这是单个边界快照，不等同整局结算 `34486`。

## 特殊伤害证据

- 明确观察到 `ThrowAttack`：`8` 条、诊断和 `2412.876`，来自两个短生命周期实体，均回溯到玩家实体。
- 明确观察到 `Un_Common_ExplosionDamage`：`1` 条、`298.471`；边界的 `throw=298.471` 与它完全相等。它很可能对应作者描述的爆炸桶或同局爆炸效果，但仅凭这一份日志不能确定具体目标。
- 玩家本体以外的所有短生命周期攻击实体均能回溯到玩家；本局没有出现 B 组噩梦娃娃那种“owner 指向自身”的独立召唤实体。
- 承伤逐击为 `45.158`，而结算显示 `46`。作者观察到受伤后回复 1 点，这说明结算承伤与净生命变化/探针字段可能不是同一口径；当前不能将 `mRealHPDamage` 直接等同结算承伤整数。

## 当前裁决

1. **无召唤物逐击可见性 PASS**。
2. **投掷与爆炸类型可区分 PASS（类型层）**；具体道具/爆炸桶身份尚未绑定。
3. **逐击 `mRealHPDamage` 直接累加为结算伤害 FAIL**：A、B 都出现正差值。
4. **过量伤害为首要假设但未证实**：需要命中前目标 HP 或游戏自身裁剪后的字段作为证伪手段。
5. **承伤口径未闭合**：需要分离来袭伤害、实际扣血、即时回复和结算累计。

## 下一步

- 在探针中补充命中前目标 HP、命中后目标 HP、事件目标是否可计入结算，以及更稳定的房间 identity。
- 先用一个短房间验证“`min(逐击伤害, 命中前 HP)` 是否接近官方结算”；未验证前禁止使用经验倍率校准。
- NPC、木桩与召唤物组合仍保留为后续独立对照，不能与本组混算。
