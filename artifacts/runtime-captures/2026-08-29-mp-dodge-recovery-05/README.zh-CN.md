# 闪避回蓝误差样本（2026-08-29）

## 原始观察

- 作者完成一局后，结算总伤害与承伤基本一致；HUD 显示法力消耗 `2,602`、恢复 `+2,433`。
- 角色同时携带闪避回复法力的衣服与魂石；差额不是单纯的 UI 取整。
- 原始截图：`author-settlement.png`；原始日志：`BepInEx-LogOutput.log`。

## 回放裁决

- 0.4.1 在同一根调用中先看到官方耗蓝，再看到 MP 正向净变化时，把后者全部当成官方覆盖量抵扣。
- 回放找到 41 个这类被漏记的恢复段，raw 合计 `169.1415`。
- 0.4.2 使用 `after - before + pairedSpend` 重建同根净恢复；回放恢复合计 `2602.2174`，与该局耗蓝合计在 UI 边界同为 `2,602`。
- 该回放证明旧差额可由这一分支解释，并为算法提供正控；它不替代 0.4.2 的真实游戏实测。
