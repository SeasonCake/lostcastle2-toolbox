# 0.4.2 法力整数显示差 2 实测（2026-08-29）

- 作者截图：HUD 法力消耗 `720`、恢复 `+722`；随后关闭游戏和工具箱，exact 进程连续两次均为 0。
- 最终 `BepInEx/LogOutput.log`：`15,806` 字节，SHA-256 `97F78E541F292B022658A43C009A78F0E61CC4DB65509A9368725D4051401A63`。
- 消耗为 15 次 `48`，raw 合计精确 `720`。
- 有效恢复为 7 次 `96` 加一次 `49.655174255371094`，raw 合计 `721.6551742553711`；HUD 在累计末端四舍五入为 `722`，raw 净值为 `+1.6551742553710938`。
- 部分恢复的真实端点是 `100.3448257446289 -> 150`；本局 MP 上限/当前值还跨房间经历 `145 -> 150 -> 115 -> 120 -> 130`，所以恢复总量不要求与消耗总量恒等。
- `runtime_gain` 为 0，Bridge MP error/stack mismatch 为 0；没有证据支持漏记或双计。结论：`720 / 722` 是底层小数累计后的正确整数显示，不修改算法或包。
- 本局正向恢复事件的 `same_operation_spend_raw` 均为 0，因此它验证 0.4.2 常规累计与无双计，不单独替代历史日志对“同根耗蓝+回蓝”分支的回放正控。
