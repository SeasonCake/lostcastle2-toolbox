# 0.4.0 回蓝缺口与 0.4.1 官方分支回归

记录时间：2026-08-28（Asia/Shanghai）

## 0.4.0 决定性反例

- 禁咒羊皮纸局中，作者截图确认房间系统把 MP 恢复到满值 `130/130`，HUD 恢复仍为 `0`。
- `session_start` 后日志记录 6 次官方耗蓝、原始合计 `207`，没有有效官方恢复回调；后续耗蓝前的 `last_observed_raw` 已回到 `130`，证明底层 MP 写入真实发生且现有低层 Hook 已覆盖。
- 结论：实际回蓝不一定触发 `OnRecoverMana`。0.4.1 复用现有低层 MP 前后观察，对顶层真实正 delta 做兜底，并按同一根操作扣除官方回调已覆盖量；没有新增 Hook。

原始冻结日志受 `.gitignore` 排除，不随源码提交；0.4.0 反例日志 SHA-256 为 `6F59538F78D830DE92A4BE87B3D3872A37CE5340D33E59CF49917A20FB4F96C5`。

## 0.4.1 已完成回归

- 作者重启后继续同一羊皮纸存档，在硝石荒漠诅咒房消耗并回满。
- 2 次官方耗蓝原始合计 `114`；18 次官方恢复原始合计 `114`；HUD 显示法力消耗 `114`、恢复 `+114`。
- 日志中 `runtime_gain=0`，说明该局走官方恢复分支，官方事件与底层观察没有双计。
- BepInEx 加载、命名管道和完整地区显示同时正常；作者确认游戏表现正常。

裁决：0.4.1 官方恢复分支为 `R-PASS`。该局没有覆盖“官方 callback 缺失时由底层正 delta 补记”的正控，因此兜底 arm 保持 `NOT RUN`；以后只在自然再次出现缺 callback 回满时被动确认，不要求作者强造。
