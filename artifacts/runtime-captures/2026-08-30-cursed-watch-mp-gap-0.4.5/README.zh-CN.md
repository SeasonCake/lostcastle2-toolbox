# 0.4.5 零变化观察通用回蓝漏记

- 实机时间：2026-08-30；游戏与工具箱均保持运行，只复制日志，没有部署或修改游戏目录。
- 来源日志快照：`LogOutput.live-copy.log`，164,635 B，SHA-256 `961FE43752BB422B40988FA87C2B141305CFBD287AB380DBE3BD606D1E33FEF7`（日志文件本体按仓库 ignore 合同不提交）。
- 正控：取得“被诅咒的怀表”后，游戏每 20 秒从低法力回满；HUD 在截图时为法力消耗 `336`、恢复 `0`。
- 决定性序列：消耗事件 5 为 `current_raw=4 / last_observed_raw=52`，事件 6 为 `current_raw=52 / last_observed_raw=100`，其间没有 `kind=recovery` 或 `kind=runtime_gain`；随后同一交替模式继续出现。
- 源码核对：0.4.5 的零变化 `UpdateMp` 已计算 fallback 并更新满蓝基线，但 `requested=0 && effective=0` 的提前返回没有考虑 `aggregateFallback`，因此把回满事件静默丢弃。
- 无怀表正控：作者再次新开一局、尚未取得怀表，HUD 在截图时消耗 `144`、恢复 `0`，游戏法力为 `88/110`。快照 `LogOutput.no-watch-live-copy.log` 为 313,008 B / SHA-256 `A8BCD946727B1B048BA486D6C751083504CBC3A072B11938558F22B4A2240AB6`；同一 session 的前四次官方消耗均为 48，其中第二次消耗前基线已从 57 恢复到 `81.35785675`，第三/四次消耗前又恢复到 110，但 recovery/runtime_gain 均为 0。
- 因此该缺陷不是怀表专属；怀表只是最明显的满蓝样本。0.4.6 修复的是所有已被连续观察识别、却因外层调用零请求/零净变化而被提前返回丢弃的恢复。
- 生命周期：`R-FAIL`。0.4.6 源码候选只调整提前返回条件，不增加 Hook/道具分支；部署与真实复测仍未执行。
